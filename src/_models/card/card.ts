export class Card {
    constructor(title: string, text: string, src: string) {
        this.title = title;
        this.text = text;
        this.src = src;
    }

    public title: string;
    public text: string;
    public src: string;
}
